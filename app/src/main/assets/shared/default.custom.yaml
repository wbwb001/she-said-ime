# 她说输入法 — 默认方案补丁
# 覆盖 prelude/default.yaml 的方案列表，确保首次打开就是拼音输入
# 注意：Rime 的补丁文件顶层必须是 patch: 包裹

patch:
  schema_list:
    - schema: luna_pinyin
    - schema: luna_pinyin_simp
    - schema: luna_pinyin_fluency
    - schema: stroke
  default_schema: luna_pinyin
